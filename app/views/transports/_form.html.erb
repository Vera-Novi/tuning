<%= simple_form_for(transports) do |f| %>
  <%= f.input :photo, as: :file %>
<% end %>

<%= simple_form_for(transports) do |f| %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
<% end %>
