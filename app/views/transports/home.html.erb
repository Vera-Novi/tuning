<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://res.cloudinary.com/dojiqdbe4/image/upload/v1677600746/diable1_wubyti.png);">
  <div class="container">
    <p><span data-controller="typed-js"></span></p>
  </div>
</div>

<div class="containers">
  <%= link_to 'SE CONNECTER', new_user_session_path, class: 'btn' %>
  <%= link_to "SINSCRIRE",  new_user_registration_path, class: 'btn' %>
  <%= link_to "SE DECONNECTER", destroy_user_session_path, data: {turbo_method: :delete}, class: 'btn' %>
</div>

<div class="container">
  <div class="cards-container">
    <% @transports.each do |transport| %>
      <div class="card">
      <% if transport.photos.attached? %>
        <% transport.photos.each do |photo| %>
            <%= cl_image_tag photo.key %>
        <% end %>
      <% else %>
        <%= 'No Image' %>
      <% end %>
        <p><%= transport.brand%></p>
        <p><%= transport.year %> Années </p>
        <p><%= transport.price %> $</p>
        <%= link_to 'Book now', new_transport_booking_path(transport), class: 'nav-link' %>
      </div>
    <% end %>
  </div>
</div>
